# lab-gpu-scheduler-dashboard

WANG AXIS Lab server info plus a frontend-only 7-day GPU scheduler board.

The dashboard ping button sends notifications through EmailJS. Configure the public EmailJS browser settings in `scheduler-config.js`.
